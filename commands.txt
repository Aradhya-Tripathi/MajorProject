# Sniff
sudo netscanner sniff --sniff-count 10 --send-request --bp-filters "tcp and src github.com"
sudo netscanner sniff --sniff-count 10

# Classify IPAbuseDB
# Unsafe IP DEMO
sudo netscanner classify ip-address 103.146.202.146

# Safe IP DEMO
sudo netscanner classify ip-address 20.207.73.82

# Trace route
sudo netscanner traceroute 65.9.86.80

# Classify Topology
sudo netscanner classify intermediate-node 65.9.86.80

# Classify as it comes
sudo netscanner classify network-traffic --sniff-count 5